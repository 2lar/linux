# Commands for everything to learn about

## basic terminal / shell things

clear current shell session history
`history -c`

clearing history file
`> ~/.bash_history`

clearing both session and file
`history -c && > ~/.bash_history && history -w`
